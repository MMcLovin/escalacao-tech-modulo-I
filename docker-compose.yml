version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: avaliacao_redis
    ports:
      - "6379:6379"
    networks:
      - avaliacao-network

  zookeeper:
    # Atualizado de 7.3.0 para 7.5.0 para trazer a correção de segurança e suporte a cgroupv2 da JVM
    image: confluentinc/cp-zookeeper:7.5.0
    container_name: avaliacao_zookeeper
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
      KAFKA_OPTS: "-XX:-UseContainerSupport"
    ports:
      - "2181:2181"
    networks:
      avaliacao-network:
        aliases:
          - zookeeper

  kafka:
    # Atualizado de 7.3.0 para 7.5.0 para alinhar com o Zookeeper corrigido
    image: confluentinc/cp-kafka:7.5.0
    container_name: avaliacao_kafka
    depends_on:
      - zookeeper
    ports:
      - "9092:9092"
      - "29092:29092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092,PLAINTEXT_HOST://localhost:29092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_OPTS: "-XX:-UseContainerSupport"
    networks:
      - avaliacao-network

networks:
  avaliacao-network:
    driver: bridge